name: visus
description: A sample command-line application.
version: 1.0.0
# homepage: https://www.example.com
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
#   path: ^1.8.0
  visus_tokenizer:
    path: ./packages/visus-tokenizer
  visus_scanner:
    path: ./packages/visus-scanner
  visus_parser:
    path: ./packages/visus-parser
  visus_compiler:
    path: ./packages/visus-compiler

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
